apiVersion: v1
kind: Service
metadata:
  namespace: fuel-monitoring
  name: fuel-monitoring-service
spec:
  selector:
    app: fuel-monitoring-service
  ports:
    - port: 3000
      targetPort: 3000
